# Scraping
httpx
beautifulsoup4
lxml

# PDF extraction
pymupdf

# Vector DB — pgvector (producción)
pgvector
psycopg2-binary
sqlalchemy

# Embeddings multilingüe (384-dim, soporte español)
sentence-transformers

# API
fastapi
uvicorn[standard]
python-multipart

# LLM — Gemini via OpenAI-compatible SDK
openai

# Config
python-dotenv
